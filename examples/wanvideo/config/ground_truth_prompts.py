SYSTEM_PROMPT = """Create a short animation that edits this exact, still photograph according to the instruction.


Scene & Motion:
• The photo stays completely still — no camera motion, flicker, zoom, or lighting change.
• The edit is applied smoothly and consistently over time.
• Only the edited content changes — everything else stays frozen.

Instruction:
"""
